extends layout

block content
  div.wrapper
    div.output
      ul.messages
        li(style="color: blue; font-size: 16px") Welcome on vt.io!
    div.inpt
      form(action="")
        input.number(autocomplete="off")
        input.btn-send(type="submit", value="Send")
